# CGEV11 W3.2 — CGVE2 / APRENDE / NEXT-ITERATION HANDOFF

**Snapshot timestamp:** 2026-09-14T11:33:00+02:00 (Europe/Madrid)  
**Repository:** `rotprods/cos-graph-engine`  
**Authority mode:** `OBSERVATION_ONLY / HANDOFF_READY`  
**Rule:** this document is a cold-start snapshot, not permission to trust stale state. Every successor must re-read live GitHub + Linear before mutation.

---

## 0. OPERATIONAL IDENTITY / FENCE

Platform-internal ChatGPT session/chat identifiers are not exposed to the available execution tools and MUST NOT be fabricated.

- `PLATFORM_SESSION_ID = NOT_EXPOSED`
- `OPERATIONAL_SESSION_ID = ses_5cb620b4-62c8-4191-b2c9-b52c2d9f874a`
- `PLATFORM_CHAT_ID = NOT_EXPOSED`
- `OPERATIONAL_CHAT_ID = chat_c042e922-a1c2-4341-b828-b4327916c3c4`
- `BRANCH_ID = branch_28638015-6fbc-42d9-8221-9763a776cc1f`
- `SOURCE_BRANCH = integration/cgev11-w3-2-promotion-20260912`
- `SOURCE_HEAD = 3b5586127b3067ee6849bace0a9e8ed0803896c1`
- `SOURCE_PR = #119`
- `HANDOFF_ID = handoff_ea2d37b2-d8d5-4cb3-81c5-eb5ff6095b3b`
- `CORRELATION_ID = corr_9c37ae19-60b2-49e1-aba6-0a4f7a69490d`
- `CLAIM_ID = claim_d29200a9-b6cb-4193-a5b1-cb8540124f28`
- `HANDOFF_BRANCH = handoff/cgev11-w3-2-20260914-handoff-ea2d37b2`

Successor fencing contract:

1. Re-read `main`, `SOURCE_BRANCH`, #119, ROT-87 and ROT-121 before claiming work.
2. If `SOURCE_HEAD` moved, treat this snapshot as stale and reconcile rather than overwriting.
3. Never force-push qualified W1B/W1C/W1D/W2 authority branches.
4. A changed candidate invalidates exact-candidate GREEN evidence.
5. Do not merge, deploy or claim global verification solely because this handoff exists.

---

# /CGVE2 — COLD-START STATE MACHINE

## 1. RETROSPECT

The W3.2 promotion process correctly used fail-closed exact-candidate qualification instead of relying on ancestor badges. That discipline discovered three different failure classes and prevented them from being conflated:

### RED A — stale W3.1 promotion harness, not product regression

- PR #118 synthetic candidate: `9a81b670744d13940f104410b858d716d0ef78d8`
- Recovery run: `34716438806`
- Node26 functional/security/full canonical suite: PASS
- Node22 same gates: PASS until integrated coverage
- old coverage: `79.39 / 80.20 / 85.66 / 79.39`
- unchanged strongest floor: `80.05 / 80.05 / 85.80 / 80.05`
- RED artifact: `10303834859`
- digest: `sha256:e55f834bb692acc092747267ab98674afd71aefabde3cc7c166046f4885f7ffe`

Root cause: W3.1 coverage/scope controls predated qualified W2 operator/auth + HTTP surfaces. W2 had already qualified the correct corpus at `80.73 / 80.11 / 86.15 / 80.73`. #118 was closed unmerged and preserved as RED provenance.

### RED B — real product correctness bug exposed by promotion gauntlet

- prior #119 exact synthetic candidate: `df70d95b14309b67b901e0d67cf467c66ef8a106`
- Recovery run: `34716830968`
- Node26 job: `103615481314`
- exact failure: `P7: main latency = 100 (got 101)`
- pipeline summary: `61 PASS / 1 FAIL`
- artifact: `10305495229`
- digest: `sha256:b209c96b503932df4b176e66cb872e7c02606b341d597820f2f5ad9f36ac5c9f`

Root cause: `PipelineL4L5L6.traceToCallGraph()` copied authoritative structured `entry.duration`, then `CallGraphBuilder.exitCall()` added converter `Date.now()` overhead. Usually overhead rounded to 0ms; exact Node26 execution exposed +1ms corruption.

Bounded repair under ROT-121 intentionally preserves dynamic `CallGraphBuilder` wall-clock profiling and repairs precedence only in the structured-trace adapter:

- product repair commit: `5d4df4dd5c8eb369f4d1f4a0d40101516a88019e`
- deterministic regression commit: `e8a40ca12075f3049c317f7b69c98109dfdb2b40`
- qualification wiring commit: `9938d769dee51f685f4994d53cb767cc22629721`

Regression forces `Date.now()` to advance by 17ms while preserving exact recorded `100 -> 100`, verifies missing-duration default semantics and preserves explicit `0ms`.

### RED C — current control-plane false positive

Current exact candidate after timing repair:

- source head: `3b5586127b3067ee6849bace0a9e8ed0803896c1`
- exact PR synthetic merge SHA: `504093cbbd296672e44019b97a312bc6cfe4a7ef`

`COS Main Convergence Gate` run `34717390364` is fully GREEN, including:

- quality-core `103616997077`
- Docker production runtime/HTTP `103616997089`
- coverage `103616997102`
- specialized `103616997103`
- performance `103616997109`
- contract `103616997117`
- aggregate complete `103617230538`
- coverage artifact `10304879714`
- digest `sha256:ef3ef9898e30597417e19cf807eb6a9ed57f524ea37890b8c0e3cb773160e786`

`CGEV11 Recovery Stack Integration` run `34717390363` reached GREEN product/security evidence but failed the final Node22 anti-bypass textual scan:

- Node22 job `103617011645`: failure only at final anti-bypass control
- Node26 job `103617011717`: SUCCESS
- stack-complete `103617203911`: failure because Node22 result was not SUCCESS
- Node22 artifact `10304956360`
- Node22 digest `sha256:6bd804e85bebfb4f1830e3d4ade588421814ad752b121a5b763b0e3ec008a17e`
- Node26 artifact `10305555866`
- Node26 digest `sha256:7e87c7e9f8532ed9114329668aa5a5bc1240681b6791e1bead353a0b90172347`

Before that final false positive, Node22 passed:

- exact ancestry and Node `22.12.0`
- pinned sandbox TCB `node@sha256:ef24c5053d50fdc3e4e56eb4e7ddb7861874ab0fdc797046ba897581deb8e868`
- strict TypeScript
- W1B / W1C / W1D
- W2 operator/auth `10/10`
- W2 HTTP trust boundary `18/18`
- W2 production-security AST checker
- W3.2 timing regression (`69 passed, 0 failed` in L4→L5→L6 suite)
- full canonical suite
- integrated coverage
- HIGH dependency audit

Current exact Node22 integrated coverage:

- statements `80.74%`
- branches `80.09%`
- functions `86.15%`
- lines `80.74%`
- floor `80.05 / 80.05 / 85.80 / 80.05`
- delta `+0.69 / +0.04 / +0.35 / +0.69`
- `npm audit`: `0 vulnerabilities`

The W2 AST checker passed and scanned the relevant production changes including `packages/graph/src/pipeline-l4l5l6.ts`.

The final raw grep then scanned `W1D_SHA...HEAD`, which necessarily contained the already-qualified detector source `scripts/check-w2-production-security.mjs`. It matched the detector's own diagnostic literals:

- `@ts-ignore/@ts-nocheck is forbidden`
- `new Function() is forbidden`

and emitted `Integration candidate introduced a forbidden code/type bypass or dynamic-code primitive`.

This is a detector self-hit, not evidence of a production bypass.

---

## 2. BOOTSTRAP — LIVE TRUTH AS OF HANDOFF

### Git authority

- `main = 3ae197ebe6024b68ea2cc33a4c54c76fbc8d1e83`
- `main` reports `protected:true`; exact admin-level protection visibility may be connector-limited and must be re-read before promotion.
- W3.1 prequalified head: `04e6222a7541d14ffed66eef16de5bd8f59720e6`
- W2 qualified source: `3ff1609cf7270e393f9fa0f7e89da97fbf0a6651`
- W2 exact synthetic candidate: `1a67e2c733510dcec2f9973c37365a5b4c1af330`
- W2 run `34713960872`: PASS
- current W3.2 source branch head: `3b5586127b3067ee6849bace0a9e8ed0803896c1`
- PR #119: CLOSED, DRAFT, UNMERGED, mergeable at last read; closure is a CI-cost control while the scanner repair is batched.
- #116 and #117 remain historical/open draft authority lanes; do not promote them from stale badges.
- #118 is closed/unmerged RED provenance.

### Linear authority

- ROT-23 — W3 Architectural reconciliation: `In Progress`
- ROT-87 — W3.2 promotion: `In Progress`, Urgent
- ROT-121 — deterministic structured-trace timing precedence: `In Progress`, Urgent, child of ROT-87
- ROT-120 — real-environment secret inventory/rotation: separate operational obligation, not a repository-code blocker unless new evidence reopens security.

W3 parent DoD remains:

1. one canonical API per responsibility;
2. no unresolved stacked-branch contradiction;
3. no critical half-migration;
4. final integration path to `main` topologically valid and fully GREEN.

Passing W3.2 does not automatically authorize deployment, production certification or global `CGEV11_SUBLIME_STATE_VERIFIED`.

---

## 3. ULTRA PLAN — NEXT BOUNDED ITERATION

North Star for the next agent:

> Produce one new exact W3.2 candidate in which the anti-bypass control no longer self-triggers, without weakening semantic security coverage, then requalify the entire candidate from scratch and only advance promotion if all current authorities agree.

Ordered graph:

`LIVE_TRUTH_REBIND -> CLAIM/FENCE -> SCANNER_REPAIR -> EVIDENCE_UPDATE -> DIFF_AUDIT -> REOPEN_DRAFT -> DUAL_EXACT_GATES -> ARTIFACT_SEAL -> LIVE_MAIN_RECHECK -> ROT-121_CLOSE_IF_PROVEN -> ROT-87_CLOSE_IF_PROVEN -> READY_FOR_REVIEW_IF_AUTHORIZED`

Preferred minimal control-plane repair already recorded in PR #119:

1. Preserve full `W1D_SHA...HEAD` mutation allowlist.
2. Preserve and rerun the W2 production-security AST checker.
3. Preserve fail-open workflow checks and all floors.
4. Scope only the simple textual "new bypass introduced" diff/grep to `W2_SHA...HEAD`, so already-qualified W2 checker literals cannot self-trigger.
5. Do not modify product code in this batch unless a fresh RED demonstrates a new product defect.
6. Update `docs/hardening/integration/W3_2_PROMOTION_EVIDENCE.md` with the self-hit evidence and repair rationale.

---

## 4. SPRINT — WORK COMPLETED BY THIS SESSION

This session:

- closed W2 repository-security scope with exact evidence;
- separated external credential inventory/rotation into ROT-120;
- activated W3.2;
- created preserved RED preflight #118 instead of blindly promoting ancestor #116;
- reconciled W3.1 promotion controls with qualified W2;
- opened W3.2 #119;
- classified the missing `W3_1_BASE_SHA` failure as harness precondition and repaired only the precondition;
- discovered the real structured timing corruption rather than tolerating `101 ~= 100`;
- created ROT-121;
- repaired timing-source precedence and added deterministic adversarial regression;
- reran exact candidate qualification;
- proved Convergence 7/7 GREEN and Recovery GREEN through product/security/coverage/audit;
- isolated the remaining failure to the anti-bypass detector scanning its own literals;
- closed #119 temporarily to prevent unnecessary CI spend during the final repair;
- created this handoff branch without modifying the W3.2 source branch.

---

## 5. VERIFY — INVARIANTS THE SUCCESSOR MUST PRESERVE

The successor MUST NOT:

- lower coverage floors;
- weaken exact timing assertions or introduce tolerances to hide the 100→101 class;
- remove W2 AST production-security scanning;
- remove full W1D→HEAD mutation allowlisting;
- add `continue-on-error` or equivalent fail-open behavior;
- force-push authority branches;
- trust prior GREEN after changing candidate bytes;
- merge #119 merely because Convergence was GREEN on `504093cb...`;
- claim production/deployment/global seal from repository qualification alone.

The successor MUST prove on one fresh exact synthetic SHA:

- Convergence aggregate GREEN;
- Docker production runtime + HTTP health GREEN;
- performance benchmark GREEN;
- Recovery Node22 GREEN;
- Recovery Node26 GREEN;
- W1B/W1C/W1D/W2 GREEN;
- W3.2 timing regression GREEN;
- integrated coverage >= strongest floors;
- HIGH audit zero;
- anti-bypass + workflow-safety GREEN;
- exact-SHA aggregate receipts GREEN.

---

# /APRENDE — DURABLE LEARNINGS

1. **Exact-candidate gates are discovery instruments, not bureaucracy.** They found both stale control-plane coverage and a nondeterministic 1ms product defect that ancestor GREEN badges could not prove away.
2. **Classify every RED before patching.** Product defect, harness precondition, environment block and detector false positive require different repairs. Treating them as one class creates unsafe shortcuts.
3. **Never manufacture GREEN by reducing the oracle.** Coverage ratchets, exact timing semantics, audit severity and sandbox authority remained unchanged; only the code/harness was corrected.
4. **Security scanners must not confuse detector source with prohibited production behavior.** A raw diff grep over code that contains its own forbidden-pattern literals is structurally self-referential. Preserve semantic AST authority and scope coarse textual scanning to the new mutation boundary.
5. **Explicit checker preconditions are part of the contract.** `W3_1_BASE_SHA` was required by the W2 AST checker; missing it is a harness bug, not a security bug.
6. **A PR can be deliberately closed to save CI spend without abandoning the candidate.** Batch intermediate repair commits with the PR closed, then reopen one complete candidate and pay for one meaningful qualification wave.
7. **Any candidate-byte change invalidates exact GREEN.** Evidence belongs to a SHA, not a branch name, PR number or narrative.
8. **Keep external operational unknowns separate from repository correctness.** Real deployment secret inventory belongs to ROT-120 unless evidence proves it invalidates repository security.
9. **Preserve RED evidence.** #118 and the timing RED are useful permanent provenance; neither should be erased after repair.
10. **Cold-start handoffs must begin with live-truth reconciliation.** This document records a point in time, not a future authority claim.

`/aprende` status: lessons harvested, authorities separated, resume point explicit, successor contract bounded.

---

# /NEXT-ITERATION-METAPROMPT

```text
CGEV11 / CGVE2 NEXT ITERATION — W3.2 FINAL PROMOTION CONTROL CLOSURE

IDENTITY / COLD START
- Repository: rotprods/cos-graph-engine
- Handoff: handoff_ea2d37b2-d8d5-4cb3-81c5-eb5ff6095b3b
- Correlation: corr_9c37ae19-60b2-49e1-aba6-0a4f7a69490d
- Prior source branch: integration/cgev11-w3-2-promotion-20260912
- Snapshot source head: 3b5586127b3067ee6849bace0a9e8ed0803896c1
- Snapshot main: 3ae197ebe6024b68ea2cc33a4c54c76fbc8d1e83
- PR #119 was CLOSED/DRAFT/UNMERGED to avoid CI spend during repair.
- Linear: ROT-87 + ROT-121 In Progress.

MISSION
Finish W3.2 without weakening any oracle. The only known repository blocker at handoff is the final Recovery Node22 anti-bypass textual scanner self-matching the already-qualified W2 detector strings. All product/security/runtime checks had passed on exact synthetic 504093cbbd296672e44019b97a312bc6cfe4a7ef; Convergence run 34717390364 was 7/7 GREEN. Recovery 34717390363 was GREEN through coverage/HIGH audit and failed only the final self-referential grep. This historical GREEN/RED is evidence only; any new bytes require a fresh candidate.

MANDATORY FIRST ACTIONS
1. Re-read live main, the source branch, PR #119, ROT-87 and ROT-121. Do not trust this handoff if live truth differs.
2. Confirm the source branch head. If it moved from 3b558612..., investigate ownership/concurrency before mutation. Never force-push authority branches.
3. Inspect the current Recovery workflow and exact prior failure log before editing.

BOUNDED REPAIR
4. Apply ONLY the minimal control-plane scanner repair unless a new RED proves a product defect:
   - preserve full W1D_SHA...HEAD mutation allowlist;
   - preserve W2 production-security AST checker and rerun it;
   - preserve workflow fail-open checks;
   - preserve all W1D/W2/timing/security/coverage/audit gates;
   - scope the coarse textual new-bypass diff/grep to W2_SHA...HEAD so already-qualified W2 checker diagnostic literals cannot self-trigger.
5. Update docs/hardening/integration/W3_2_PROMOTION_EVIDENCE.md with the exact false-positive evidence and repair rationale.
6. Compare W2_SHA...HEAD and verify no unintended product/source mutations beyond the already-owned structured timing repair + regression. If unexpected files appear, STOP and reconcile.

QUALIFICATION
7. Only after the complete repair batch is present, reopen PR #119 as DRAFT. Do not run partial CI candidates unnecessarily.
8. Require fresh COS Main Convergence Gate and fresh CGEV11 Recovery Stack Integration on the new exact synthetic merge candidate.
9. Both gates must correspond to the same exact candidate identity. Require:
   - Convergence complete aggregate GREEN;
   - Docker production runtime/HTTP health GREEN;
   - performance GREEN;
   - Recovery Node22 + Node26 GREEN;
   - W1B/W1C/W1D/W2 GREEN;
   - deterministic structured-trace timing regression GREEN;
   - integrated coverage >= 80.05/80.05/85.80/80.05;
   - npm audit HIGH = 0;
   - anti-bypass and workflow-safety GREEN;
   - exact-SHA receipts GREEN.
10. Persist run IDs, job IDs, artifact IDs, SHA256 digests and exact coverage metrics in evidence + Linear.

PROMOTION DECISION
11. After GREEN, re-read live main. If main moved, reconcile and requalify; do not carry GREEN forward.
12. Re-read PR reviews/threads and repository protection/rulesets. Unknown protection state is UNKNOWN, not PASS.
13. Close ROT-121 only if the final exact candidate retains the deterministic timing contract.
14. Close ROT-87 only if both exact gates are GREEN, main is reconciled, and there is no unresolved P0/P1/review/protection blocker.
15. Move #119 to Ready for Review only if W3 parent DoD and repository controls authorize it. Do NOT automatically merge, deploy, certify production or claim CGEV11_SUBLIME_STATE_VERIFIED.
16. If work remains, emit a new cold-start handoff with exact resume point.

STOP CONDITIONS
- main moved unexpectedly;
- source branch moved under another agent;
- exact SHA mismatch between authority evidence and candidate;
- new product/security RED;
- coverage below floor;
- HIGH audit > 0;
- Docker or performance failure;
- unresolved review/protection blocker or protection visibility insufficient for a promotion claim;
- unexpected W2...HEAD mutation.

OPERATING RULE
OBSERVED > ASSUMED. Exact SHA > branch badge. Preserve RED evidence. No force rewrite. No weakened oracle. No stale GREEN.
```

---

## 6. HANDOFF / RESUME POINT

The next agent should begin at **scanner repair**, not repeat W2, not reopen the timing bug, and not blindly rerun historical CI.

Exact resume statement:

> `main` was still `3ae197e...`; W3.2 source was `3b558612...`; PR #119 was closed/draft/unmerged; exact candidate `504093cb...` had Convergence 7/7 GREEN and Recovery GREEN through coverage + HIGH audit, with the only known repository blocker being the raw W1D→HEAD anti-bypass grep matching the W2 AST checker's own forbidden-pattern diagnostic strings. Perform a minimal control-plane scope repair, update evidence, then create one fresh exact dual-gate qualification candidate.

This handoff itself is informational. The source branch remains untouched by the handoff branch.

**listo para archivar ✅**
