# CLAUDE — COS Graph Engine

Read `README_FIRST.md` before doing anything else.

Current mode is reconciliation, not feature expansion. The authoritative task is to reconcile PR #34 and #35 from #33, close issue #39 findings, then rebuild W13 from the reconciled candidate.

Apply these rules on every material proposal:
1. `/leydekidlin`: define facts, assumptions, unknowns, constraints, success and falsifiable failure.
2. `/leydegilbert`: discover the best viable path using the repo, prior art and available tools.
3. `how.complexsystems.fail`: model latent conditions, coupled failures, degraded states, defenses, near misses and change-induced risk.

Do not delete >50 lines without the deletion ledger. Do not adapt legacy tests merely to fit new behavior. Do not merge or certify from narrative confidence. `Authority = min(Build, Assurance)` and only evidence raises Assurance.

Persist material state in `STATE.md`, `HANDOFF.md`, Drive Acta and the dedicated COS Todoist project.