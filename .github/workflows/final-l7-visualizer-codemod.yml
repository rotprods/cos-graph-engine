name: Final L7 visualizer codemod

on:
  push:
    branches: [fix/strict-typecheck-regressions]
    paths:
      - '.github/workflows/final-l7-visualizer-codemod.yml'
      - 'scripts/final-l7-visualizer-codemod.py'

permissions:
  contents: write

jobs:
  codemod:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/strict-typecheck-regressions
          fetch-depth: 0
      - run: python scripts/final-l7-visualizer-codemod.py
      - name: Commit and self-delete
        run: |
          rm scripts/final-l7-visualizer-codemod.py
          rm .github/workflows/final-l7-visualizer-codemod.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add packages/graph/src/visualizer.ts scripts/final-l7-visualizer-codemod.py .github/workflows/final-l7-visualizer-codemod.yml
          git commit -m 'fix(graph): derive L7 visualizer metrics from graph data'
          git push origin HEAD:fix/strict-typecheck-regressions
