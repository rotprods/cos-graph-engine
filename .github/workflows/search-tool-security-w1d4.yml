name: Search Tool Security W1D4

on:
  push:
    branches: [fix/search-tool-w1d4]
  pull_request:
    branches: [fix/tool-capability-isolation-w1d]
    paths:
      - 'packages/execution/src/tool-runtime.ts'
      - 'scripts/test-search-tool-w1d4.cjs'
      - '.github/workflows/search-tool-security-w1d4.yml'
      - 'docs/hardening/search-tool/**'
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: search-tool-security-w1d4-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '22.12.0'
  W1D4_PARENT_SHA: 'aadd8a1079f5acd2d3f0f9b00af3bd6c8bc83e0b'

jobs:
  qualify:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Bind exact source SHA
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          git cat-file -e "${W1D4_PARENT_SHA}^{commit}"
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Record runtime floor
        run: |
          set -euo pipefail
          node --version | tee w1d4-node-version.log
          test "$(node --version)" = 'v22.12.0'
      - name: Strict TypeScript
        run: npx --no-install tsc --noEmit
      - name: Preserve qualified registry boundary
        run: |
          set -euo pipefail
          node --import tsx scripts/test-tool-capability-w1d.cjs 2>&1 | tee w1d4-registry-regression.log
      - name: W1D4 SearchTool security contract
        run: |
          set -euo pipefail
          node --import tsx scripts/test-search-tool-w1d4.cjs 2>&1 | tee w1d4-search-contract.log
      - name: Full canonical test suite
        run: |
          set -euo pipefail
          npm run test:all 2>&1 | tee w1d4-test-all.log
      - name: High-severity dependency audit
        run: |
          set -euo pipefail
          npm audit --audit-level=high 2>&1 | tee w1d4-npm-audit.log
      - name: Scope and anti-bypass policy
        run: |
          set -euo pipefail
          git diff --unified=0 "$W1D4_PARENT_SHA"...HEAD -- '*.ts' '*.cjs' > w1d4-source.diff
          if grep -E '^\+.*(as any|@ts-ignore|@ts-nocheck|continue-on-error|new Function\(|eval\(|console\.log[[:space:]]*=)' w1d4-source.diff; then
            echo 'W1D4 introduced a forbidden bypass or dynamic-code primitive' >&2
            exit 1
          fi
          while IFS= read -r file; do
            case "$file" in
              packages/execution/src/tool-runtime.ts|scripts/test-search-tool-w1d4.cjs|.github/workflows/search-tool-security-w1d4.yml|docs/hardening/search-tool/W1D4_RED_EVIDENCE.md|docs/hardening/search-tool/W1D4_GREEN_EVIDENCE.md)
                ;;
              *)
                echo "Unexpected W1D4 scope mutation: $file" >&2
                exit 1
                ;;
            esac
          done < <(git diff --name-only "$W1D4_PARENT_SHA"...HEAD)
      - name: Preserve W1D4 evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: search-tool-security-w1d4-${{ github.sha }}
          path: |
            w1d4-node-version.log
            w1d4-registry-regression.log
            w1d4-search-contract.log
            w1d4-test-all.log
            w1d4-npm-audit.log
            w1d4-source.diff
          if-no-files-found: warn
