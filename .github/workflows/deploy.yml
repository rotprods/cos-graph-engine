# Deliberately failing manual guard; not a simulated successful production operation.
name: Deploy
'on':
  workflow_dispatch: {}
permissions: {}
jobs:
  convergence-freeze:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
    - name: Reject production action while convergence is incomplete
      shell: bash
      run: |
        printf 'Deploy is blocked by COS main-convergence issue #39. No external action was performed.\n' >&2
        exit 1
