name: Deploy — convergence freeze
on:
  workflow_dispatch: {}
permissions: {}
jobs:
  blocked:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: |
          echo 'Deploy is blocked by COS main convergence issue #39. No external action performed.' >&2
          exit 1
