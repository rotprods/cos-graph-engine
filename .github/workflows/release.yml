name: Release — convergence freeze
on:
  workflow_dispatch: {}
permissions: {}
jobs:
  blocked:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: |
          echo 'Release is blocked by COS main convergence issue #39. No package, tag or branch mutation performed.' >&2
          exit 1
